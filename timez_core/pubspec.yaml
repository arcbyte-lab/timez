name: timez_core
description: Pure Dart engine for Timez — no Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  timezone: ^0.10.0
  uuid: ^4.0.0
  freezed_annotation: ^2.4.0

dev_dependencies:
  test: ^1.25.0
  freezed: ^2.5.0
  build_runner: ^2.4.0
  lints: ^4.0.0