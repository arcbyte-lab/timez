class Tag {
  final String id, name, colorArgb, sortOrder, createdAt, updatedAt, deletedAt;

  Tag(
    this.id,
    this.name,
    this.colorArgb,
    this.sortOrder,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  );
}
